import random, sys, os, math
for i in range(5):
    print(random.randint(1, 10))    # randint is in the random module
                                    # you must call the random with a '.' to search for the function
    
